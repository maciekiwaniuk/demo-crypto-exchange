{% extends 'base.html.twig' %}

{% block title %}Welcome page{% endblock %}

{% block body %}
<div class="container bg-blue-100">
    <h1>Welcome page</h1>

    <br>
    
    <a class="btn btn-blue" href="{{ path('app_login') }}">Login</a>

    <br>
    <br>

    <a href="{{ path('app_register') }}">Register</a>
</div>
{% endblock %}

